// Détection intelligente par AI simulée
function detecterAI(data) {
  console.log('🔎 Analyse AI en cours...');
  const score = Math.random() * 100;
  console.log(`🧠 Score d'intelligence calculé : ${score.toFixed(2)}%`);
  return score;
}
